# Netlify SPA fallback: deep links (e.g. /dashboard/organizations) must serve the React app.
# Status 200 = rewrite (URL stays the same; no redirect loop). Static files in /static still resolve first.
/*    /index.html   200
